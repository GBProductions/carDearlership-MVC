@using CarDealership.Models;

<h1>Car List</h1>
<ul>
  @if (Model.Count == 0)
  {
    <p>There are no items in the list.</p>
  }
  @foreach (Car car in Model)
  {
    <li>@car.Model, has @car.Miles miles, and costs only $@car.Price!</li>
  }
</ul>

<a href="/cars/new">Add a new car.</a>